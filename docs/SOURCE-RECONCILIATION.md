# Source reconciliation

Primary scope: `Pasted markdown(3).md`, including the Local Lead Engine MVP and three-manual-run gate. Outreach behavior: `Pasted markdown (2)(2).md`. Operator course context: `Pasted markdown (3)(1).md`.

The connected GitHub source was read from `DevinAiUk/AiSuite`, base commit `d7296006ab416bc94528eb5f7070841c2b465ca9`, under `system-factory/`. The uploaded AiSuite and AiGLeadSuite ZIPs contain the same application route/file set. The `looperAi` connector lookup returned 404; the uploaded LooperAI ZIP supplied its ingestion, humanization and outbound source.

The original app had split in-memory/database state, mandatory database credentials at import, static skill placeholders, duplicated nested sidebars, a simulated export manifest and no generated content for new projects. The revision unifies state, generates actual drafts, makes the studios editable and produces real local bundles.

The growth-loop archive informed source attribution, the seven-folder delivery separation, signature verification requirements, no credentials in exports, strict validation before side effects, versioned handoff and truthful status language. The Make reliability report informed consent, deduplication, bounded retries, correlation IDs and human escalation. Historical account/connection IDs and credentials were not copied into application templates.

Scope decisions:

- Local Lead Engine only; three complete fictional demos are clearly labeled and cannot satisfy the client-run gate.
- E-commerce, authority and operations templates remain deferred. Completing three runs records readiness but does not silently add a broader product.
- The goal is an initial client-review draft within 15 minutes; this is not a verified setup-time or outcome guarantee.
- Native Google/Notion delivery and Make/n8n/Zapier activation remain mock/stub adapters, as the primary spec requests.
- An implementation specification is explicitly distinguished from a native Make blueprint.
- A single-operator durable-disk deployment is supported; a multi-tenant production service requires database/auth integration.
